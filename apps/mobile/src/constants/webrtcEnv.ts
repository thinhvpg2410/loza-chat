// Use || instead of ?? so empty string "" also falls back to the default
export const TURN_USERNAME =
  process.env.EXPO_PUBLIC_TURN_USERNAME || "0646bc697c8d494f4aac97fd";
export const TURN_CREDENTIAL =
  process.env.EXPO_PUBLIC_TURN_CREDENTIAL || "uueIw+M9v8Vtg9Q8";
