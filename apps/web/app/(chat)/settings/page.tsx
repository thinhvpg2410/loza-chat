export default function SettingsPage() {
  return (
    <main className="flex min-h-0 flex-1 flex-col overflow-y-auto bg-[var(--zalo-chat-bg)] p-6">
      <h1 className="text-lg font-semibold text-[var(--zalo-text)]">Settings</h1>
      <p className="mt-2 max-w-xl text-sm text-[var(--zalo-text-muted)]">
        Profile and preferences will live here. Phase W1 provides layout and routing only.
      </p>
    </main>
  );
}
